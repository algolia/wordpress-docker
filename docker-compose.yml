wordpress:
  build: .
  ports:
    - 80:80
  links:
    - db:mysql
  volumes:
    - ./wordpress:/var/www/html
  environment:
    WP_VERSION: 4.6.1

db:
  image: mysql:5.5
  environment:
    MYSQL_ROOT_PASSWORD: password
