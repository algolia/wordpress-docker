wordpress:
  build: .
  ports:
    - 80:80
  links:
    - db:mysql
  volumes:
    - ./wordpress:/var/www/html
  dns: 208.67.222.222
  environment:
    WP_VERSION: 5.3.2

db:
  image: mysql:latest
  environment:
    MYSQL_ROOT_PASSWORD: password
